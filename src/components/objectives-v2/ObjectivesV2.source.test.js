import test from"node:test";import assert from"node:assert/strict";import{readFile}from"node:fs/promises";import{resolve}from"node:path";const component=resolve(process.cwd(),"src/components/objectives-v2/ObjectivesV2.jsx"),styles=resolve(process.cwd(),"src/components/objectives-v2/ObjectivesV2.css");
test("ObjectivesV2 consumes the existing hook and mutations",async()=>{const content=await readFile(component,"utf8");for(const item of["useObjectives","addObjective","updateObjective","deleteObjective","ObjectiveForm"])assert.equal(content.includes(item),true);assert.doesNotMatch(content,/firebase|Firestore|addDoc|updateDoc|deleteDoc/)});
test("ObjectivesV2 exposes the premium responsive objective cockpit",async()=>{const[content,css]=await Promise.all([readFile(component,"utf8"),readFile(styles,"utf8")]);for(const label of["Gestion personnelle","Objectifs actifs","Épargne visée","Progression moyenne","Objectifs atteints","Nouvel objectif","Progression globale","Prochain objectif","En avance","En cours","À risque","Atteint","Aucun objectif.","Créer mon premier objectif"])assert.equal(content.includes(label),true);assert.match(css,/@media\(max-width:620px\)/);assert.match(css,/min-height:44px/);assert.match(css,/prefers-reduced-motion:reduce/);assert.doesNotMatch(css,/overflow-x:auto|min-width:620px/)});
