import test from"node:test";import assert from"node:assert/strict";import{readFile}from"node:fs/promises";import{resolve}from"node:path";const component=resolve(process.cwd(),"src/components/quotes-v2/QuotesV2.jsx"),cssPath=resolve(process.cwd(),"src/components/quotes-v2/QuotesV2.css");test("QuotesV2 consumes only existing quote hooks and mutations",async()=>{const source=await readFile(component,"utf8");for(const contract of["useWorkQuotes","useProfessionalActivities","useThirdParties","addQuote","editQuote","deleteQuote"])assert.match(source,new RegExp(contract));assert.doesNotMatch(source,/firebase|firestore|collection\(|setDoc|addDoc/)});test("QuotesV2 exposes only real statuses and responsive V2 states",async()=>{const[source,css]=await Promise.all([readFile(component,"utf8"),readFile(cssPath,"utf8")]);for(const label of["Organisation","Devis","Nombre de devis","Montant total","En attente","Acceptés","Nouveau devis","Taux d'acceptation","Aucun devis."])assert.match(source,new RegExp(label));assert.doesNotMatch(source,/status:\s*"(?:draft|sent|refused)"|value="(?:draft|sent|refused)"|Brouillon|Envoyé|Refusé/);assert.match(css,/prefers-reduced-motion/);assert.doesNotMatch(css,/overflow-x:\s*(auto|scroll)/)});
