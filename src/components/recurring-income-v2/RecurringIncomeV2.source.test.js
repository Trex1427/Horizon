import test from"node:test";import assert from"node:assert/strict";import{readFile}from"node:fs/promises";import{resolve}from"node:path";const component=resolve(process.cwd(),"src/components/recurring-income-v2/RecurringIncomeV2.jsx"),styles=resolve(process.cwd(),"src/components/recurring-income-v2/RecurringIncomeV2.css");
test("RecurringIncomeV2 consumes existing hooks, utility and mutations",async()=>{const content=await readFile(component,"utf8");for(const item of["useRecurringIncome","useAccounts","useCategories","getRecurringIncomeApplicableAmount","addRecurringIncome","updateRecurringIncome","deleteRecurringIncome","RecurringIncomeForm"])assert.equal(content.includes(item),true);assert.doesNotMatch(content,/firebase|Firestore|addDoc|updateDoc|deleteDoc/)});
test("RecurringIncomeV2 exposes the complete premium responsive interface",async()=>{const[content,css]=await Promise.all([readFile(component,"utf8"),readFile(styles,"utf8")]);for(const label of["Gestion financière","Revenus récurrents","Montant mensuel","Prochaine échéance","Revenus annuels","Ajouter un revenu","Revenus attendus","Répartition","Mensuel","Hebdomadaire","Annuel","Autre","Actif","Suspendu","Aucun revenu récurrent.","Créer un revenu"])assert.equal(content.includes(label),true);assert.match(css,/@media\(max-width:620px\)/);assert.match(css,/min-height:44px/);assert.match(css,/prefers-reduced-motion:reduce/);assert.doesNotMatch(css,/overflow-x:auto|min-width:620px/)});
