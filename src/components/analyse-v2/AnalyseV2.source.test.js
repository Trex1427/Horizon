import test from "node:test";import assert from "node:assert/strict";import{readFile}from"node:fs/promises";import{resolve}from"node:path";
const component=resolve(process.cwd(),"src/components/analyse-v2/AnalyseV2.jsx"),styles=resolve(process.cwd(),"src/components/analyse-v2/AnalyseV2.css");
test("AnalyseV2 consumes only existing analytical data",async()=>{const content=await readFile(component,"utf8");for(const item of["buildAnalysisSnapshot","getPeriodRange","getPreviousPeriodRange","useTransactionsContext","useFixedExpenses","useRecurringIncome","useAccounts","useCategories","useSubcategories"])assert.equal(content.includes(item),true);assert.doesNotMatch(content,/firebase|Firestore|addDoc|updateDoc|deleteDoc/)});
test("AnalyseV2 exposes the requested cockpit and responsive states",async()=>{const[content,css]=await Promise.all([readFile(component,"utf8"),readFile(styles,"utf8")]);for(const label of["Revenus de la période","Dépenses de la période","Épargne nette","Solde net","Évolution des finances","Votre capacité d’épargne actuelle","Répartition des dépenses","Top catégories","Revenus","Dépenses","Épargne","Points d’attention","Aucun point d’attention"])assert.equal(content.includes(label),true);assert.match(css,/min-height:330px/);assert.match(css,/@media\(max-width:620px\)/);assert.match(css,/min-height:44px/);assert.match(css,/\.analyse-v2-kpis\{order:2\}/);assert.match(css,/prefers-reduced-motion:reduce/);assert.doesNotMatch(css,/overflow-x:auto|min-width:620px/)});
