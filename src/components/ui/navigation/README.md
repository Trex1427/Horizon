# Navigation

Role:
- Handles movement and context switching in the UI.

Components present:
- Sidebar
- BottomNavigation
- ActionBar

When to use:
- Use when users navigate between sections or trigger contextual actions.

Rules:
- Navigation stays domain-agnostic.
- Routing is delegated to caller callbacks.
- Tout nouveau composant UI doit appartenir a une famille.
