# Filters

Role:
- Centralizes filter and sort patterns.

Components present:
- AppFilterBar
- AppFilterDialog
- AppSortDialog

When to use:
- Use for list filtering and ordering controls.

Rules:
- Keep filter UX consistent between pages.
- Keep implementation presentation-only.
- Tout nouveau composant UI doit appartenir a une famille.
