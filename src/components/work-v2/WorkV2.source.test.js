import test from"node:test";import assert from"node:assert/strict";import{readFile}from"node:fs/promises";import{resolve}from"node:path";const component=resolve(process.cwd(),"src/components/work-v2/WorkV2.jsx"),cssPath=resolve(process.cwd(),"src/components/work-v2/WorkV2.css");test("WorkV2 consumes existing professional hooks and metrics",async()=>{const source=await readFile(component,"utf8");for(const contract of["useProfessionalActivities","useWorkProjects","useWorkProjectTransactions","useThirdParties","calculateWorkProjectMetrics"])assert.match(source,new RegExp(contract));assert.doesNotMatch(source,/firebase|firestore|collection\(|setDoc|addDoc/)});test("WorkV2 exposes honest responsive activity states",async()=>{const[source,css]=await Promise.all([readFile(component,"utf8"),readFile(cssPath,"utf8")]);for(const label of["Organisation","Travail","Chantiers actifs","Chiffre d'affaires","Temps planifié","Tâches","À venir","Activité professionnelle","Aucune échéance.","Aucune activité."])assert.match(source,new RegExp(label));assert.doesNotMatch(source,/removeProfessionalActivity|deleteProfessionalActivity/);assert.match(css,/prefers-reduced-motion/);assert.doesNotMatch(css,/overflow-x:\s*(auto|scroll)/)});
